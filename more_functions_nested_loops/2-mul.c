#include "main.h"

/**
 * mul - Write a function that multiplies two integers.
 * @a: int type number
 * @b: int type number
 * Return: 1 if is digit, else 0
 */
int mul(int a, int b)
{
	return (a * b);

}


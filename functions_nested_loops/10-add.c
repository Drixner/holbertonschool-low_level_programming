#include "main.h"

/**
 * add - Write a function that adds two integers.
 * @a : variable operator
 * @b : variable operator
 * Return: result
 */
int add(int a, int b)
{
	int suma = (a + b);

	return (suma);
}

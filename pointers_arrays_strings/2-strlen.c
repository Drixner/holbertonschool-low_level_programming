#include "main.h"
#include <string.h>
/**
 *_strlen - Write a function that returns the length of a string.
 * @s : varible char
 * Return: int de pointers len
 */

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
